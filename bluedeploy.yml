apiVersion: apps/v1
kind: Deployment
metadata:
  name: reactapp-test
spec:
  selector:
    matchLabels:
      app: bluereactapp
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: bluereactapp
    spec:
      containers:
        - name: bluereactapp
          image: abhibhatia28/testreact
          ports:
            - containerPort: 80