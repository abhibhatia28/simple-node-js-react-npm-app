apiVersion: apps/v1
kind: Deployment
metadata:
  name: reactapp-test
spec:
  selector:
    matchLabels:
      app: bluereactapp
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: bluereactapp
    spec:
      containers:
        - name: bluereactapp
          image: abhibhatia/reactcapstoneabbb
          ports:
            - containerPort: 80